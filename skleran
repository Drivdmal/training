from skleran.model_selection import train_test_split

x_train, x_test, y_train, y_test =
train_test_split(data_x, data_y, test_size=.5, stratify=test['x'] - стратифицировать по количеству (чтобы соходилоаьс)

data_sample['x'] = data['x'].sample(frac=0.2, random_state=42)
