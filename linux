sudo umount -l /dev/sdb3
sudo mount -w /dev/sdb3 /home/drivdmal/d/

Terminal Lynux:
pwd - узнать имя (путь) каталога
ls - узнать что в текущем каталоге
    ls -a - покажет все файлы, в т.ч. невидимые (их имена начинаются с точки .)
    ls - lR - файлы в данной дирректории и во всех вложенных
    ls -l -R / > list.txt 2> /dev/null - список всех файлов в системе,
 					 за исключением тех каталогов на
					 чтение которых нет прав;
                                         сообщения об ошибках будут
					 ингнорироваться
cp опции файл_источник файл_приемник - копировать файл
или
cp опции файл_источник директория_приемник - копировать файл
rm - удалить файл
mv - переименовать/переместить файл
mkdir - создание директории
rmdir - удаление директории
touch - создание файла или установка нового времени модификации
less - просмотр содержимого файла с пейджингом
cd - смена каталога
cd .. - переход в предыдущий каталог
cd - без параметров - в каталог пользователья
history - история ввода команд
history | less - поэкранный вывод информации (о вводе команд)
                 причем у каждой команды в истории есть номер
    !23 - выполнит команду под номером 23
    !! - последнюю введенную команду
    !:0 - отдельные слова из команды
    !abc - выполнит команду начинающуюся на abc
ps ax - узнать все процессы
jobs - выводит список выполняемых фоновых задач
ps axu - все процессы с владельцами
kill №_process - завершает принудительно процесс по номеру
kill -KILL 2763 - завершит процесс 2763 (вместо KILL можно TERM, INT)
chmod 644 file.c - права доступа для файла в 8-ричном формате
                   (права записи только у владельца, права чтения у всех)
chmod a+x myscript - делает файл myscript исполняемым
chmod go-rwx * снимает со всех файлов в текущем каталоге все прова,
	       кроме владельца
chmod -R u+rwX,go=rX - если испорчены права доступа в своей домашней
                       директории, та команда, скорее всего,
                       приведет все в удовлетворительное состояние
